# InternSim - Integrated Intern Simulation System

## 🎯 Proje Hakkında

InternSim, stajyer yönetimi ve AI destekli değerlendirme sistemidir. Bu proje 4 kişi tarafından ayrı ayrı geliştirilmiş modüllerin entegrasyonundan oluşmuştur.

### Özellikler
- 🔐 Kullanıcı kimlik doğrulama (JWT)
- 👥 Stajyer yönetimi
- 📋 Görev atama ve takip
- 📝 Görev gönderimi
- ⭐ AI destekli görev değerlendirme
- 💬 AI Code Review & Chat sistemi
- 📊 Dashboard ve raporlama

## 🏗️ Mimari

```
TumProje/
├── backend/              # Ana Backend (FastAPI + PostgreSQL)
│   ├── backend/
│   │   └── app/
│   │       ├── routes/   # API endpoints
│   │       ├── models/   # Database models
│   │       ├── services/ # AI services
│   │       └── ai/       # AI prompts & configs
│   └── requirements.txt
├── frontend/            # React Frontend
│   └── frontend/
│       ├── src/
│       └── package.json
├── _archive/            # Arşivlenmiş eski modüller
├── .env                 # Global Environment variables (Backend & Frontend)
└── start-system.ps1     # Sistem başlatma scripti
```

## 🚀 Kurulum

### Gereksinimler
- Python 3.8+
- Node.js 16+
- PostgreSQL 12+

### 1. PostgreSQL Veritabanı Kurulumu

```powershell
# PostgreSQL'e bağlan
psql -U postgres

# Veritabanı oluştur
CREATE DATABASE internsim;

# Çıkış
\q
```

### 2. Global Environment (.env) Kurulumu

Ana dizinde `.env` dosyası oluşturun ve aşağıdaki değişkenleri güncelleyin:

```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=internsim
DB_USER=postgres
DB_PASSWORD=1234

# JWT Secret (Güvenli bir key oluşturun)
SECRET_KEY=<auto-generated>
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# io.net API Configuration (AI Servisleri için)
IO_NET_API_KEY=<your-api-key>
IO_NET_MODEL_ID=meta-llama/Llama-3.3-70B-Instruct
IO_NET_API_URL=https://api.intelligence.io.solutions/api/v1/chat/completions

# CORS Configuration
BACKEND_CORS_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:3000

# Frontend Configuration
VITE_API_URL=http://localhost:8000
```

### 3. Backend Kurulumu

```powershell
cd backend

# Virtual environment oluştur (eğer yoksa)
python -m venv venv

# Aktive et
.\venv\Scripts\Activate.ps1

# Bağımlılıkları yükle
pip install -r requirements.txt
```

### 4. Frontend Kurulumu

```powershell
cd frontend\frontend

# Node modüllerini yükle
npm install
```

## ▶️ Sistemi Çalıştırma

### Otomatik Başlatma (Önerilen)

```powershell
# Ana dizinde
.\start-system.ps1
```

Bu script hem backend hem frontend'i ayrı terminallerde otomatik başlatır.

### Manuel Başlatma

**Backend:**
```powershell
cd backend
.\venv\Scripts\Activate.ps1
python -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000
```

**Frontend:**
```powershell
cd frontend\frontend
npm run dev
```

## 🌐 Erişim Adresleri

- **Frontend**: http://localhost:8080
- **Backend API**: http://localhost:8000
- **Swagger UI (API Docs)**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

## 📡 API Endpoints

### Auth
- `POST /auth/register` - Kullanıcı kaydı
- `POST /auth/login` - Giriş yap
- `GET /auth/me` - Mevcut kullanıcı

### Internship
- `GET /internships` - Stajları listele
- `POST /internships` - Staj oluştur
- `GET /internships/{id}` - Staj detayı

### Task
- `GET /tasks` - Görevleri listele
- `POST /tasks` - Görev oluştur
- `PUT /tasks/{id}` - Görev güncelle

### Submission
- `POST /submissions` - Görev gönder
- `GET /submissions` - Gönderimleri listele

### Evaluation
- `POST /evaluations` - Değerlendirme yap
- `GET /evaluations/{id}` - Değerlendirme detayı

### AI Services
- `POST /ai-mentor/review` - AI Code Review (Chat destekli)
- `POST /ai-feedback/evaluate` - AI Görev Değerlendirme
- `GET /ai-mentor/health` - AI Mentor health check
- `GET /ai-feedback/health` - AI Feedback health check

## 🧪 Test

### Backend API Test

```powershell
# Health check
curl http://localhost:8000/

# Database bağlantısı test
curl http://localhost:8000/db-test

# AI Mentor test
curl -X POST http://localhost:8000/ai-mentor/review `
  -H "Content-Type: application/json" `
  -d '{"code": "def hello():\n    print(\"Hello\")", "context": "Simple Python function"}'
```

### Frontend Test
Browser'da http://localhost:8080 adresini açın.

## 🔧 Yapılandırma

Tüm yapılandırmalar ana dizindeki `.env` dosyasından yönetilir.

### Backend .env & Frontend .env (Birleşik)
```env
# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=internsim
DB_USER=postgres
DB_PASSWORD=1234

# JWT Secret
SECRET_KEY=<auto-generated>
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AI Services
IO_NET_API_KEY=<your-api-key>
IO_NET_MODEL_ID=meta-llama/Llama-3.3-70B-Instruct

# CORS
BACKEND_CORS_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:3000

# Frontend
VITE_API_URL=http://localhost:8000
```

## 📁 Modül Geçmişi

Orijinal 4 modül `_archive/` klasöründe saklanmıştır:
- `ai-mentor/` - AI Code Review sistemi
- `ai-feedback/` - AI Görev değerlendirme sistemi
- İki identik backend modülü birleştirildi

## 🛠️ Teknoloji Stack

### Backend
- **Framework**: FastAPI 0.128.0
- **Database**: PostgreSQL + SQLAlchemy 2.0.45
- **Authentication**: JWT (python-jose)
- **AI**: io.net API (Llama-3.3-70B-Instruct)
- **Server**: Uvicorn 0.40.0

### Frontend
- **Framework**: React 18.3.1
- **Build Tool**: Vite 5.4.19
- **Language**: TypeScript 5.8.3
- **UI Library**: Shadcn/UI + Radix UI
- **Styling**: TailwindCSS 3.4.17
- **Routing**: React Router 6.30.1
- **State**: TanStack Query 5.83.0


## 🐛 Sorun Giderme

### Backend başlamıyor
1. Virtual environment aktif mi kontrol edin
2. PostgreSQL çalışıyor mu kontrol edin
3. .env dosyasındaki DB bilgilerini kontrol edin

### Frontend başlamıyor
1. `node_modules` yüklü mü kontrol edin
2. `npm install` komutunu tekrar çalıştırın

### Database bağlanmıyor
1. PostgreSQL servisinin çalıştığından emin olun
2. Database'in oluşturulduğunu kontrol edin: `psql -U postgres -c "\l"`
3. Şifrenin doğru olduğunu kontrol edin

### AI servisleri çalışmıyor
1. .env dosyasında `IO_NET_API_KEY` ve `IOINTELLIGENCE_API_KEY` değerlerini kontrol edin
2. API key'in geçerli olduğundan emin olun

## 📝 Geliştirme Notları

### Virtual Environment Yeniden Oluşturma
Eğer projeyi taşıdıysanız venv'i yeniden oluşturun:

```powershell
cd backend
Remove-Item -Recurse -Force venv
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

## 📄 Lisans

Bu proje eğitim amaçlı geliştirilmiştir.

## 👥 Katkıda Bulunanlar

4 kişilik ekip tarafından geliştirilmiştir:
- Backend modülü geliştiricisi
- Frontend modülü geliştiricisi
- AI Feedback sistem geliştiricisi
- AI Mentor sistem geliştiricisi

Entegrasyon: AI Assistant
